<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
    <link rel="stylesheet" href="style.css">
</head>

<body class="has-background-primary-light">
    <div class="wrapper">
        <header>
            <h1 class="has-text-centered">
                <a href="/" class="is-uppercase has-text-weight-bold is-size-1">Tech Blog</a>
            </h1>
            <nav class="navbar has-background-primary-dark">
                {{#if loggedIn}}
                <a href="/" class="button is-info has-text-black">Home</a>
                <a href="/dashboard" class="button is-info has-text-black">Dashboard</a>
                <button id="logout" class="button is-danger has-text-black">Logout</button>
                {{else}}
                <a href="/login" class="button is-success has-text-black">Login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer class="has-text-centered">
            Made with ❤️ by 1ance-301
        </footer>
    </div>
    {{#if loggedIn}}
    <script src="/public/javascript/logout.js"></script>
    {{/if}}
</body>

</html>